# 迭代记录

## v0.1.0 — 项目初始化（阶段0）

**日期：** 2026-06-23 · **commit:** `424677c`

- [x] 创建工程骨架：server (Spring Boot)、admin-web (Vue3)、screen-web (Vue3)、face-service (Python)
- [x] 初始化 CLAUDE.md 编码规范
- [x] 编写设计文档：需求整理、功能需求、页面布局、数据库设计 SQL
- [x] 准备公共资源：字体文件、Excel 导入模板、CSV 数据

## v0.2.0 — 后端基础设施（阶段1）

**日期：** 2026-06-23 · **commit:** `1570cdf`

- [x] Spring Boot 3.x + JDK 17 + MyBatis-Plus + Spring Security (JWT) 项目搭建
- [x] 统一返回体 `R<T>`、分页 `PageResult<T>`、全局异常处理
- [x] JWT 认证过滤器 + 登录接口 + 角色权限控制（admin/college/academic）
- [x] CORS 配置、MyBatis 配置、Redis 缓存配置
- [x] 数据库初始化 SQL（建表 + 种子数据）
- [x] 通用组件：AOP 操作日志、数据权限基类、文件存储服务

## v0.3.0 — 核心业务（阶段2）

**日期：** 2026-06-23 · **commit:** `4ccca53`

- [x] 校友管理：CRUD + 分页查询 + Excel 批量导入（含冲突检测）
- [x] 资料档案：CRUD + 媒体附件上传/删除/排序 + 分类/学院关联
- [x] **审核状态机**：draft → pending_college → pending_academic → approved → published ⇄ unpublished
- [x] 学院审核 / 教务处审核：通过（可选意见）/ 驳回（必带意见）
- [x] 资料库运营：上架/下架/置顶/推荐/排序 + 轮播方案 CRUD
- [x] 字典管理：学院/专业/资料分类

## v0.4.0 — 大屏API + 人脸识别（阶段3+4）

**日期：** 2026-06-23 · **commit:** `bf87720`

- [x] 大屏只读 API（匿名）：默认轮播方案 + 校友墙分页 + 校友成长轨迹 + 全文搜索
- [x] ArcSoft 虹软 SDK 集成（Python ctypes WinDLL，仅 Windows x64）
- [x] `/extract` 人脸特征提取 + `/match` 1:N 比对 + `/health` 健康检查
- [x] Java 端 FaceService：特征加密存储(AES) + 录入/识别接口
- [x] 识别结果：HIT（返回校友+timeline）/ NO_MATCH / DEGRADED

## v0.5.0 — 管理后台（阶段5.1-5.4）

**日期：** 2026-06-23 · **commit:** `d7a897c`

- [x] Vue3 + Vite + Pinia + Element Plus 工程搭建
- [x] 登录页 + JWT 存储 + 路由守卫（角色权限）
- [x] 工作台仪表盘：统计卡片 + 分类占比(ECharts) + 审核流转 + 待办列表
- [x] 校友管理页：表格分页 + 搜索筛选 + 新增/编辑弹窗 + Excel 导入 + 人脸录入按钮
- [x] 资料档案页：表格 + 搜索 + 新建/编辑（关联校友、分类、学院、媒体上传）
- [x] 审核中心页：待办列表 + 通过/驳回操作 + 审核记录查看
- [x] 资料库运营页：已入库档案池 + 上架/下架/置顶/推荐操作 + 轮播方案管理
- [x] 系统管理页：学院/专业 CRUD + 用户管理 + 操作日志/登录日志查看
- [x] 字体集成：HarmonyOS_SansSC_Bold/Regular

## v0.6.0 — 大屏展示端（阶段5.5-5.7）

**日期：** 2026-06-24 · **commit:** `5d16ce9`

- [x] Vue3 + Vite + Pinia 轻量工程（无 Element Plus，独立 CSS）
- [x] 深色科技风主题：粒子背景 + 蓝色/金色点缀 + 渐变透明面板
- [x] 默认轮播页：自动轮播 + 上一条/下一条导航 + 进度条 + 过渡动效
- [x] 校友墙页：网格分页展示 + 悬浮高亮 + 搜索
- [x] 校友详情页：成长轨迹 timeline + 媒体展示
- [x] 搜索页：全文搜索 + 结果列表
- [x] FaceOverlay 人脸覆盖层：摄像头采集 + 扫描动画 + 命中欢迎过场
- [x] `useScale` 组合式函数：1920×1080 自适应等比缩放
- [x] 字体集成：din-bold-2（数字）、Rajdhani-SemiBold（英文标签）
- [x] Chrome Kiosk 全屏部署方案

## v0.7.0 — 系统管理与收尾（阶段6）

**日期：** 2026-06-24 · **commit:** `a4a2a61`

- [x] 仪表盘完善：统计卡片实时数据 + ECharts 分类占比图
- [x] 账号管理：用户 CRUD + 重置密码 + 启停账号
- [x] 操作日志：分页查询 + 操作人/模块/类型/时间筛选
- [x] 登录日志：分页查询 + 登录人/IP/状态/时间筛选

## v0.8.0 — 修复与优化

**日期：** 2026-06-24 · **commits:** `4a59ae6` / `a7250ff` / `8799c98` / `7423f3c` / `c64c2f8`

- [x] `fix(face-service)`: 补充 `_h_engine` 全局变量声明
- [x] `fix(server)`: 人脸特征编码从 `List<Float>` 改为 base64 字符串（避免 ArcSoft 二进制含 NaN 致 JSON 崩溃）
- [x] `fix(face-service)`: 特征编码同步改为 base64，删除 float↔bytes 序列化工具函数
- [x] `fix(server)`: 新增 WebMvcConfig 映射 `/uploads/**` 静态资源 + SecurityConfig 放行
- [x] `fix(admin-web)`: 审核空 body `null`→`{}` 修复 400 错误；头像上传参数修正；资料档案 editId/isEdit 改为 computed
- [x] `fix(admin-web)`: 轮播方案从 radio-group 改为卡片选择 UI；vite 代理添加 `/uploads`
- [x] `refactor(screen-web)`: 人脸识别 UI 重构 — 全屏扫描环→右下角小指示器；命中→右侧滑入卡片；移除 miss/degraded 负向提示
- [x] `fix(screen-web)`: timeline 循环限制 2 轮防止卡死；autoPlayPausedByHit 标记防止识别循环重置定时器
